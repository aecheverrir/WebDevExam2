/*
import { Meteor } from "meteor/meteor";
import { fetch } from ""

Meteor.methods({
    "nextbus.get"() {
        return 
        fetch("http://webservices.nextbus.com/service/publicJSONFeed?command=vehicleLocations&a=sf-muni&=N&t=0", {method: 'GET'})
        .then((res => {
            return res.json()
        })).then((json) {
            return json;
        })
    }
});

*/